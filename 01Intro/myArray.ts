const cricketers: string[] =[];
const jerseyNumber: number[] =[];

type User ={
    name:string,
    isActive:boolean
}

const allUser : User[]=[];

allUser.push({name:"",isActive:true});

const MLModels: number [][] = [
    [255,255,255]
]